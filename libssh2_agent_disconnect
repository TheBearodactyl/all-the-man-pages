libssh2_agent_disconnect(3)                                                                   libssh2 manual                                                                   libssh2_agent_disconnect(3)

NAME
       libssh2_agent_disconnect - close a connection to an ssh-agent

SYNOPSIS
       #include <libssh2.h>

       int libssh2_agent_disconnect(LIBSSH2_AGENT *agent);

DESCRIPTION
       Close a connection to an ssh-agent.

RETURN VALUE
       Returns 0 if succeeded, or a negative value for error.

AVAILABILITY
       Added in libssh2 1.2

SEE ALSO
       libssh2_agent_connect(3) libssh2_agent_free(3)

libssh2 1.2                                                                                     23 Dec 2009                                                                    libssh2_agent_disconnect(3)
