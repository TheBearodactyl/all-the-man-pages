libssh2_channel_ignore_extended_data(3)                                                       libssh2 manual                                                       libssh2_channel_ignore_extended_data(3)

NAME
       libssh2_channel_ignore_extended_data - convenience macro for libssh2_channel_handle_extended_data(3) calls

SYNOPSIS
       #include <libssh2.h>

       libssh2_channel_ignore_extended_data(arguments)

DESCRIPTION
       This function is deprecated. Use the libssh2_channel_handle_extended_data2(3) function instead!

       This is a macro defined in a public libssh2 header file that is using the underlying function libssh2_channel_handle_extended_data(3).

RETURN VALUE
       See libssh2_channel_handle_extended_data(3)

ERRORS
       See libssh2_channel_handle_extended_data(3)

SEE ALSO
       libssh2_channel_handle_extended_data(3)

libssh2 1.2.4                                                                                   20 Feb 2010                                                        libssh2_channel_ignore_extended_data(3)
