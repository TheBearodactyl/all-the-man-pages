PIP-LIST(1)                           pip                          PIP-LIST(1)

NAME
       pip-list - description of pip list command

DESCRIPTION
       List installed packages, including editables.

       Packages are listed in a case-insensitive sorted order.

USAGE
          python -m pip list [options]

OPTIONS
       -o, --outdated
              List outdated packages

       -u, --uptodate
              List uptodate packages

       -e, --editable
              List editable projects.

       -l, --local
              If  in  a  virtualenv  that has global access, do not list glob‐
              ally-installed packages.

       --user Only output packages installed in user-site.

       --path <path>
              Restrict to the specified installation path for listing packages
              (can be used multiple times).

       --pre  Include  pre-release  and  development versions. By default, pip
              only finds stable versions.

       --format <list_format>
              Select the output format among: columns  (default),  freeze,  or
              json

       --not-required
              List packages that are not dependencies of installed packages.

       --exclude-editable
              Exclude editable package from output.

       --include-editable
              Include editable package from output.

       --exclude <package>
              Exclude specified package from the output

AUTHOR
       pip developers

COPYRIGHT
       The pip developers

23.0                             Feb 19, 2023                      PIP-LIST(1)
