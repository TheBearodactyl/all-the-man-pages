std::ranges::common_range(3Library Functions Manuastd::ranges::common_range(3)

NAME
       std::ranges::common_range - A range for which ranges::begin and
       ranges::end return the same type.

SYNOPSIS
       #include <ranges>

Concept definition
       template<typename _Tp>
       concept std::ranges::common_range =  range<_Tp> && same_as<iterator_t<_Tp>, sentinel_t<_Tp>>

Detailed Description
       A range for which ranges::begin and ranges::end return the same type.

       Definition at line 689 of file ranges.

Author
       Generated automatically by Doxygen for libstdc++ from the source code.

libstdc++                       Wed Feb 1 2023    std::ranges::common_range(3)
