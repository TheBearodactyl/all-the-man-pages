libssh2_agent_connect(3)                                                                      libssh2 manual                                                                      libssh2_agent_connect(3)

NAME
       libssh2_agent_connect - connect to an ssh-agent

SYNOPSIS
       #include <libssh2.h>

       int libssh2_agent_connect(LIBSSH2_AGENT *agent);

DESCRIPTION
       Connect to an ssh-agent running on the system.

       Call libssh2_agent_disconnect(3) to close the connection after you're doing using it.

RETURN VALUE
       Returns 0 if succeeded, or a negative value for error.

AVAILABILITY
       Added in libssh2 1.2

SEE ALSO
       libssh2_agent_init(3) libssh2_agent_disconnect(3)

libssh2 1.2                                                                                     23 Dec 2009                                                                       libssh2_agent_connect(3)
